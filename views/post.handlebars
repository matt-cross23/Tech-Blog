<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{description}}</p>
  <p>Posted by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span class="dollar-amount">Post id: {{id}}</span>
  </p>
  <p>
    <button class="btn btn-primary">Comment on this Post!</button>
  </p>
</div>
